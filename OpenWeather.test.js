test("get valid current forecast", () => {
  //   OpenWeather.instance.getCurrentConditions(4460243, result => {
  // expect(Number(result.main.temp)).toBeTruthy();
  //   });
    expect(1).toEqual(1);
});
